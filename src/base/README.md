# Base Module

Core platform abstractions: libuv event loop, signals and slots, PacketStream pipeline, handles, timers, logging, state machines, buffers, filesystem, process management, and threading.

Everything else in Icey builds on this module.

**[Full documentation →](../../doc/base.md)**
